Mirrors — 1/1 healthy
  ok — all mirrors synced recently

Brain boot — 1/1 ok
  ok — every brain commit boots

Watched mailboxes — 0/0 healthy
  ok — no watched mailboxes parked or expired

Dead-lettered runs (last 999h) — 0 total
  ok — no runs dead-lettered in window

note: alert + 'token source disabled' log inputs are not in this DB-backed view — check logs (support skill) for those.

healthy
